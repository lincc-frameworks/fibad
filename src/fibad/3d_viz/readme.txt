To start a server to serve the html page; do

python3 -m http.server 8181 --bind 0.0.0.0

Then forward the 8181 port if necessary and navigate to http://localhost:8181/



##########
To save a UMAP to the JSON format, use save_umap_to_json.py




###########
For a simpler plotly 3d plot, use save_umap_to_json.py
